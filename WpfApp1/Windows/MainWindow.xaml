<Window Name="mainWindow" x:Class="WpfApp1.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.Windows"
        xmlns:wpfApp1="clr-namespace:WpfApp1"
        xmlns:commands="clr-namespace:WpfApp1.Commands"
        mc:Ignorable="d"
        Opacity="1"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="{x:Null}"
        Title="Main Window" Height="450" Width="800" DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Window.Resources>
        <Style TargetType="{x:Type local:MainWindow}">
            <Setter Property="SpotLight."/>
        </Style>
        
    </Window.Resources>
    <Window.InputBindings>
        <KeyBinding Key="Q" Modifiers="Control" Command="commands:MyAppCommands.QuitApplication"/>
    </Window.InputBindings>
    <Viewport3DVisual>
    <DockPanel>
        <DockPanel.LayoutTransform>
            <ScaleTransform ScaleX="1.7" ScaleY="1.7"/>
        </DockPanel.LayoutTransform>
        <Menu DockPanel.Dock="Top" />
        <TabControl DockPanel.Dock="Bottom" Margin="10" Opacity=".7">
            <TabControl.Effect>
                <DropShadowEffect BlurRadius="10" Color="#FFC52C2C"/>
            </TabControl.Effect>
            <TabItem Header="Menus">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <TreeView ItemsSource="{Binding Path=(wpfApp1:AppProperties.MenuItemListCollectionView)}"
                              Grid.Row="0" Grid.Column="0">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Path=Children}">
                                <TextBlock Text="{Binding Path=Header}" />
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </Grid>
            </TabItem>
        </TabControl>
    </DockPanel>
    </Viewport3DVisual>
</Window>